package com.dev.patterns.behavioral.strategy;

/**
 * the Strategy and Context interfaces determine how well the pattern achieves its intent.
 */
public interface Strategy {
    String doOperation();
}
